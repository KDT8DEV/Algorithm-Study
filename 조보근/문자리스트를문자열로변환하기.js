/* 문자들이 담겨있는 배열 arr가 주어집니다. arr의 원소들을 순서대로 이어붙인 문자열을 return하는 solution함수를 작성해주세요.
 */

function solution(arr) {
 
   let answer = arr.join(''); //여기서 join이라는 것이 배열의 모든 원소를 하나의 문자열로 합쳐주는 함수라고 하네. 알아두면 좋겟어 이건
   return answer;
}